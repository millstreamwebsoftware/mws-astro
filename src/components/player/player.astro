---
import Video from "@shared/Video.astro";
import Section from "@layouts/Section.astro";

interface Props {
    src: string;
}

const { src } = Astro.props;
---

<Section component="player" {...Astro.props}>
    {src && <Video {src} background={false} />}
</Section>
